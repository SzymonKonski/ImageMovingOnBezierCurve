namespace BezierCurveApp
{
    public enum AnimationType
    {
        Rotation,
        MovingOnCurve
    }

    public enum RotationType
    {
        Naive,
        Filtered
    }

    public enum AnimationState
    {
        Playing,
        Stopped
    }
}